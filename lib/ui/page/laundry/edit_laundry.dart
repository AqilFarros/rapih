part of '../page.dart';

class EditLaundry extends StatelessWidget {
  const EditLaundry({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        children: [
          Text("Edit")
        ],
      ),
    );
  }
}